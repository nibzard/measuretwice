Context for dfa4990 (T033): MVP_SPEC.md section 12 draws three lines this task enforces at the provider boundary: pass only inputs declared in using, reject oversized evidence rather than truncating it, and combine no unrelated cases or access scopes into shared model state. The limit needed one honest offline token estimate. The recorded service limit is 32,000 tokens for state plus the longest question, but the adapter holds no tokenizer and any characters-per-token guess breaks on CJK or ASCII extremes. The safe direction is rejection: one token of UTF-8 text covers at least one byte, so the UTF-8 byte count bounds the token count from above, and a request inside 32,000 bytes is inside 32,000 tokens for any text. JEV_STATE_BUDGET_BYTES publishes the constant; the adapter measures JSON.stringify(state) plus JSON.stringify(question) because one request carries one question, so the longest question is the only question. The budget is inclusive: equality passes, one byte above fails, and the suite pins both edges by padding one input to the exact serialized size. The rejection throws ValidationError oversized_input at /inputs rather than returning one EvaluatorFailure, because the registry files oversized_input under validation reasons reported before execution, while the Rust run state accepts exactly three operational attempt codes (require_operational_code), so widening the failure union would ripple through the core, the report schema, and the fixtures for one adapter-local rule. Instead dispatchAssessment now keeps the thrown code and field path inside its evaluator_error message, which also upgrades the existing jevEvidenceState field rejections from the opaque 'The evaluator threw' text. Cost: the scheduler classifies the failure retryable by code, so an oversized request may spend its bounded attempts; T034 adds the wrapper-level limit validation before any attempt starts, which removes that waste. Batching stayed unimplemented: one question per request satisfies the spec rule trivially, the flagship projections differ anyway, and the suite pins the one-question shape so a later batch operation must re-justify itself against the same tests plus the identical-projected-state rule now written in providers/jev/README.md. Embedded-instruction resistance is structural, not analytical: the envelope key is fixed, the request field set is fixed, registration is host code the request cannot name, and JSON quoting keeps hostile strings as values, so the tests assert shapes and unchanged boundaries rather than model behavior.
