Context for b7f9de4 (T032): MVP_SPEC.md section 12 requires retries configured once and total time bounded across attempts and backoff, and section 5 makes the wrapper own that policy while Rust validates transitions. The classification decision: reason code alone decides retryability, because the sanitized failure message cannot drive policy (it may quote nothing) and timing adds nothing to the choice. evaluator_error and evaluator_timeout name transient conditions; invalid_assessment is permanent because the defect sits in the adapter path, so a retry would spend one attempt and return through the same broken normalization. Permanence needed one new boundary transition: fail_attempt could only requeue or exhaust, so a wrapper-only design would have to burn dummy restarts to reach a record, inflating the attempt count the report contract exposes. RunState::fail_permanent records the error at the failing attempt with the operational code and the true count; the NAPI surface and runFailPermanent mirror it. Execution.tla gained the matching disjunct of AttemptFail (the wrapper chooses retry or permanent record while attempts remain) and TLC rechecked both configurations with no error, so the model record stays honest. Backoff doubles from the base with no jitter and no cap constant: the attempt limit (max 10) bounds the doubling and the deadline wake-up bounds the wait, so one delay past the deadline never starts work; a zero base delay keeps the T030 immediate-requeue behavior and its tests. The freed slot still drains at once when a failure enters backoff, otherwise fresh queued work would idle behind one sleeping retry. A check waiting in backoff holds no core record, so cancel or deadline during backoff records the never-started skip the terminal transition already assigns; the release path disarms the backoff wake-ups so one disarmed retry never executes. The Jev adapter passes retry maxRetries 0 because the SDK defaults retry twice with no total budget: one wrapper attempt must be one SDK request or the configured attempts multiply invisibly. The trace permanent-failure carries an explicit permanent flag because the Rust boundary replay is policy-free: the trace states what the wrapper decided, the harness obeys, and the TS scheduler replay derives the same outcome from the code alone.
