Context for b642d87 (T030): MVP_SPEC.md section 5 makes the TypeScript wrapper own queues and concurrency while Rust validates every transition, and section 12 makes saturation produce skipped records instead of dropped work. The scheduler therefore keeps no scheduling state of its own beyond the queue arithmetic (active count, FIFO queue, never-started set) and states every event to the run state boundary, so the checked TLA+ invariants (ActiveWithinLimit, FreshPendingWithinLimit, AttemptsBounded, TerminalReportFrozen) hold by construction on the Rust side. The never-started rule for max_pending comes from the model record's design decision: a retry is not new work, so it must never be dropped when fresh work fills the pending slots. Retries restart without delay here because backoff belongs to T032 and timers to T031; the injected clock only computes the attempt deadline instant and the terminal time. The suite replays only the five runtime traces whose events the scheduler itself can produce, because deadline, cancellation, late-result, and duplicate-result traces need T031 or deliberately drifted wrapper behavior. Wiring into run() waits for T034 so the exact-rule path keeps its tested behavior until the semantic path lands.
