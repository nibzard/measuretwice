Context for 0fc1e55 (T034): MVP_SPEC.md section 5 splits the run path across the boundary: the wrapper sends validated projected requests to registered evaluators and owns queueing, deadlines, retries, and cancellation, while Rust owns decision rules, outcome aggregation, and report construction. Before this task run() drove the run state by hand for exact rules only, so the scheduler existed but nothing routed through it. The implementation choice was to make run() one thin composer: precompute the deterministic rule records, then hand every check to scheduleRun with one execute closure that either returns a rule record or dispatches one question and crosses one new decideQuestionCheck boundary. That keeps every bounded-execution behavior (queue, deadline, retries, late-result drops) in the one module the TLA+ record maps to the wrapper, and it moved the wrapper-level limit validation T033 promised into the scheduler gate that validates the effective execution configuration before any attempt starts. The decision had to stay in Rust per the dependency table ('no independent copies of core decision rules'), so the NAPI binding gained decide_question_check, which composes policy::decide with two new report pieces: CheckRecord::from_question and QuestionMeasurements::parse. The interesting edge is the undecidable answer: probability_mass_v0 requires reported mass, but a label-only adapter returns none, and dispatchAssessment already accepted the assessment because the assessment contract itself needs no distribution. The failure is not operational (the adapter worked), yet the run boundary accepts exactly three attempt codes and acceptResult refuses error outcomes, so the honest mapping is one permanent invalid_assessment failure whose message carries the core code and field path (missing_field at /assessment/distribution); the error record keeps no assessment and no policy because the frozen record contract says none executed. A retry would return the same answer, and the scheduler already classifies invalid_assessment as permanent, so no attempt is wasted. Totals stayed absent: the core terminal transitions build the report without them and per-check records carry usage and timing, so widening complete() for totals would touch the frozen boundary for one optional field. The scripted-adapter tests needed care with step order: the adapter consumes one step synchronously per assess call, and all checks dispatch during the admission loop, so retry steps interleave after the first-pass steps, not after their own check's first step.
